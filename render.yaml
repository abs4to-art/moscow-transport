services:
  - type: worker
    name: moscow-transport-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: VK_TOKEN
        sync: false
